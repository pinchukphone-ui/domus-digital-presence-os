# Directus

Directus запускается из закреплённого Docker image. Пользовательские коллекции создаются SQL migration при первом старте PostgreSQL и автоматически отражаются Directus. `ADMIN_TOKEN` используется только сервером Astro; не передавайте его в браузер.

