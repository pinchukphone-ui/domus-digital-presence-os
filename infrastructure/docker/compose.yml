name: domus-presence
services:
  database:
    image: domus-postgres:16.14-alpine3.24-su-exec
    build:
      context: ../..
      dockerfile: infrastructure/docker/postgres/Dockerfile
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../database/migrations/001_content_schema.sql:/docker-entrypoint-initdb.d/10-content-schema.sql:ro
      - ../database/seeds/001_mortgage_hub.sql:/docker-entrypoint-initdb.d/20-mortgage-seed.sql:ro
      - ../database/content-changes/002_service_ru_review.sql:/docker-entrypoint-initdb.d/30-service-ru-review.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 12

  directus:
    image: directus/directus:12.1.1@sha256:27fd291463f4e746a7911139377a1dbc7a5c09ae82ee15b028b97bcc4950c69d
    restart: unless-stopped
    ports: ["127.0.0.1:8055:8055"]
    depends_on:
      database: { condition: service_healthy }
    environment:
      SECRET: ${DIRECTUS_SECRET}
      ADMIN_EMAIL: ${DIRECTUS_ADMIN_EMAIL}
      ADMIN_PASSWORD: ${DIRECTUS_ADMIN_PASSWORD}
      ADMIN_TOKEN: ${DIRECTUS_ADMIN_TOKEN}
      DB_CLIENT: pg
      DB_HOST: database
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_DB}
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      PUBLIC_URL: ${DIRECTUS_PUBLIC_URL}
      TELEMETRY: "false"
      WEBSOCKETS_ENABLED: "true"
      CACHE_SCHEMA: "false"
      IP_TRUST_PROXY: "false"
    volumes:
      - ../../apps/directus/uploads:/directus/uploads
      - ../../apps/directus/snapshots:/directus/snapshots
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8055/server/ping"]
      interval: 10s
      timeout: 5s
      retries: 18
      start_period: 30s

  directus-metadata:
    image: domus-postgres:16.14-alpine3.24-su-exec
    build:
      context: ../..
      dockerfile: infrastructure/docker/postgres/Dockerfile
    depends_on:
      directus: { condition: service_healthy }
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
      DIRECTUS_PUBLIC_TOKEN: ${DIRECTUS_PUBLIC_TOKEN}
      DIRECTUS_PREVIEW_TOKEN: ${DIRECTUS_PREVIEW_TOKEN}
    volumes:
      - ../database/bootstrap:/bootstrap:ro
    command: ["psql", "-h", "database", "-U", "${POSTGRES_USER}", "-d", "${POSTGRES_DB}", "-v", "ON_ERROR_STOP=1", "-f", "/bootstrap/001_directus_metadata.sql"]

  public-web:
    build: { context: ../.., dockerfile: apps/public-web/Dockerfile }
    restart: unless-stopped
    ports: ["127.0.0.1:4321:4321"]
    depends_on:
      directus-metadata: { condition: service_completed_successfully }
    environment:
      CONTENT_SOURCE: directus
      DIRECTUS_INTERNAL_URL: http://directus:8055
      DIRECTUS_STATIC_TOKEN: ${DIRECTUS_PUBLIC_TOKEN}
      PUBLIC_SITE_URL: ${PUBLIC_SITE_URL}
      PREVIEW_MODE: "false"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:4321/healthz"]
      interval: 10s
      timeout: 5s
      retries: 12

  preview:
    build: { context: ../.., dockerfile: apps/public-web/Dockerfile }
    restart: unless-stopped
    ports: ["127.0.0.1:4322:4321"]
    depends_on:
      directus-metadata: { condition: service_completed_successfully }
    environment:
      CONTENT_SOURCE: directus
      DIRECTUS_INTERNAL_URL: http://directus:8055
      DIRECTUS_STATIC_TOKEN: ${DIRECTUS_PREVIEW_TOKEN}
      PREVIEW_SITE_URL: ${PREVIEW_SITE_URL}
      PREVIEW_MODE: "true"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:4321/healthz"]
      interval: 10s
      timeout: 5s
      retries: 12

volumes:
  postgres-data:
